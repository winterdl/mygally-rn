import React from 'react';
import {Icon} from 'react-native-elements';

import Colors from 'datas/Colors';
const FloatingButton = () => {
  return (
    <Icon name="add" type="material" color={Colors.active} onPress={() => {}} />
  );
};

export default FloatingButton;
